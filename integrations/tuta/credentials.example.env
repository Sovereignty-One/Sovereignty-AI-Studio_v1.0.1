# Copy to credentials.env, chmod 600, fill in real values.
# NEVER commit the real file.

TUTA_EMAIL=Appel420@tutamail.com
TUTA_PASSWORD=yourpassword
# TUTA_TOTP_SECRET=jbsw y3dp ehpk 3pxp   # only if 2FA enabled
