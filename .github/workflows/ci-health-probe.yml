name: CI Health Probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    name: CI health probe
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - name: Prove self-hosted job execution
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "GitHub Actions job execution is alive"
          echo "repo=${GITHUB_REPOSITORY}"
          echo "ref=${GITHUB_REF}"
          echo "sha=${GITHUB_SHA}"
          echo "runner=${RUNNER_OS}/${RUNNER_ARCH}"
          test "${GITHUB_ACTIONS}" = "true"
          test "${RUNNER_OS}" = "macos"
          echo "CI_HEALTH=PASS"
